mistakes made in version 1

* weird handrolled argument parser which doesn't work very well
* assumes people are familiar with `grep -e`
* output is a little awkward and nonconfigurable
* library usage might be nice? (split off from the cli, a TeaVM port)

good things about version 1

* good defaults imo (search all jars in current directory if none are passed)